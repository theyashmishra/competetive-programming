#include <bits/stdc++.h>
using namespace std;

int main() {
	long long int t,x;
	long long int a,b,c,d,e;
	cin>>t;
	while(t--){
	    cin>>a>>b>>c;
	    
	    if(a!=0){
	        d=(c-b)/a;
	   
	    e=(a*d)+b;
	    cout<<e<<"\n";}
	    else{
	        cout<<b<<"\n";
	    }
	}
	return 0;
}
