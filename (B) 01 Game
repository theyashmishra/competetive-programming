#include<bits/stdc++.h>
#include<math.h>
using namespace std;

int main(){
    long long int t,a,b,c,x,n;
    char s[100];
    cin>>t;
    while(t--){a=b=0;
       cin>>s;
       n=strlen(s);
       for(int i=0;i<n;i++){
           if(s[i]=='0'){
               a++;
           }
           else{
               b++;
           }
       }
       x=(a<b)?a:b;
       if(x%2!=0){
           cout<<"DA\n";
       }
       else{
           cout<<"NET\n";
       }
}

    return 0;
}
