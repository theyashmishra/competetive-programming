#include <bits/stdc++.h>
using namespace std;

int main() {
long long int t,i,n,j,x;

    cin>>n;
    long long int a[n];
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+n);
      for(i=0;i<n;i++){
      a[i]=a[i]*(n-i);
  
    }
    sort(a,a+n);
    cout<<a[n-1]<<"\n";

	return 0;
}
