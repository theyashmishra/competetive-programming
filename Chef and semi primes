#include <bits/stdc++.h>
using namespace std;
bool isquare(int n) 
{ 
    for (int i = 1; i * i <= n; i++) { 
  
        // If (i * i = n) 
        if ((n % i == 0) && (n / i == i)) { 
            return true; 
        } 
    } 
    return false; 
} 
bool isPrime(int n) 
{ 
    // Corner case 
    if (n <= 1)  return false; 
  
    // Check from 2 to n-1 
    for (int i=2; i<n; i++) 
        if (n%i == 0) 
            return false; 
  
    return true; 
} 
  
int main() {
int t,i,j,x,y,n;
cin>>t;
while(t--){y=0;
    cin>>n;
    if(n==1||isPrime(n)){
        cout<<"NO\n";
    }
   
    else{
    for(i=2;i<n/2;i++){
        if(n%i==0&&isPrime(i)){
            x=n/i;
            for(j=2;j<=x/2;j++){
                if(isPrime(j)&&isPrime(x-j)&&j!=i&&i!=(x-j)){
                    y=1;
                }
            }
        }
    }
    (y==1)?cout<<"YES\n":cout<<"NO\n";
}
    
}
	return 0;
}
