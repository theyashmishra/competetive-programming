#include <iostream>
#include<math.h>
using namespace std;

int main() {
	long long int t,a,b,c,d,x;
	cin>>t;
	while(t--){
	    cin>>a>>b>>c;
	    d=a+c;
	    b=b*2;
	    x=abs(b-d);
	    (x%2==0)?cout<<(x/2)<<"\n":cout<<(x+1)/2<<"\n";
	}
	return 0;
}
